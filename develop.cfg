[buildout]
extends = base.cfg versions.cfg
show-picked-versions = true
update-versions-file = versions.cfg

[supervisord.ini]
develop = true

[scripts]
eggs += pytest
       pytest-cov
scripts += pytest
